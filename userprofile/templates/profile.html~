{% extends "base.html" %}

{% block content %}
  
  <h2>Profile</h2>
  
  {% for field in form %}
    {{field.error}}

  {% endfor %}

  {% if userprofile.picture %}
  <p><img src="/static/assets/{{userprofile.picture}}" width="150" /> </p>
  {% endif %}
  
  <form action="/accounts/profile/" method="post" enctype="multipart/form-dataZ">{% csrf_token %}
  <ul>
  {{form.as_ul}}
  </ul>  

  <input type="Submit" value="Update" />
    
  </form>
  
{% endblock %}


